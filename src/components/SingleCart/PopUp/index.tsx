import { useQuery } from '@apollo/client'
// import { EVOLUTION_POKEMON } from '@/pages/api/hello'

const PopUp = ({id, name}: any) => {
    // const {loading, data} = useQuery(EVOLUTION_POKEMON, {
    //     variables: {id: id, name: name},
    // });
    // console.log(id, name)

    return <div>JI</div>
}
export default PopUp